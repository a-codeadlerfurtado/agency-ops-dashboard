const pages=await (await fetch('http://127.0.0.1:9333/json/list')).json();const page=pages.find(x=>x.type==='page'&&x.url.includes('readerpro'));const ws=new WebSocket(page.webSocketDebuggerUrl);await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});let s=0,p=new Map();ws.onmessage=e=>{let m=JSON.parse(e.data);if(m.id&&p.has(m.id)){let q=p.get(m.id);p.delete(m.id);m.error?q.j(m.error):q.r(m.result)}};const c=(m,a={})=>new Promise((r,j)=>{let id=++s;p.set(id,{r,j});ws.send(JSON.stringify({id,method:m,params:a}))});const sleep=ms=>new Promise(r=>setTimeout(r,ms));await c('Runtime.enable');
const before=await c('Runtime.evaluate',{returnByValue:true,expression:`({page:pageNum.value,buffer:bufferBadge.textContent,status:status.textContent})`});console.log('BEFORE',JSON.stringify(before.result.value));
await c('Runtime.evaluate',{expression:`document.getElementById('next').click()`});for(const ms of [100,500,1200]){await sleep(ms-(ms===100?0:ms===500?100:500));const r=await c('Runtime.evaluate',{returnByValue:true,expression:`({page:pageNum.value,status:status.textContent,sentences:document.querySelectorAll('.sentence').length,buffer:bufferBadge.textContent})`});console.log('T+'+ms+'ms',JSON.stringify(r.result.value));}ws.close();