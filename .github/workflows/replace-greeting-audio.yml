name: Replace greeting audio binary

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  replace-audio:
    if: contains(github.event.head_commit.message, 'stage full greeting audio')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply exact audio binary
        shell: bash
        run: |
          set -euo pipefail
          rm -f public/audio/opsquestion-greeting-full-v6.mp3
          git apply --binary .opsq/replace-audio.patch
          test "$(wc -c < public/audio/opsquestion-greeting-full-v6.mp3)" = "68812"
          echo "14bd82e3a0b0e5ce44fd78f71e9bf2a66eb18da5fdf7628d6f06769e6b04b96b  public/audio/opsquestion-greeting-full-v6.mp3" | sha256sum -c -
      - name: Commit only final MP3
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .opsq
          rm -f .github/workflows/replace-greeting-audio.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Replace greeting MP3 binary only"
          git push
