"use client";

// O Gabriel agora usa o dashboard principal com escopo global de IA/N8N.
// A rota /ai-head continua disponível para acesso direto, mas não sequestra mais
// a sessão dele nem impede acesso à Visão Geral, Foco, Saúde, TaskLog e ClickUp.
export default function AIHeadRouter() {
  return null;
}
